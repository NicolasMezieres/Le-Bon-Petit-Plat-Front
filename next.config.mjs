/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["lebonpetitplat-8af8a5c399a8.herokuapp.com", "localhost"],
  },
};

export default nextConfig;
