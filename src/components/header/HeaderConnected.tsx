import React from "react";
import Logo from "./Logo";

const HeaderConnected = () => {
  return (
    <header className="flex justify-center mt-4 md:mt-5">
      <Logo />
    </header>
  );
};

export default HeaderConnected;
